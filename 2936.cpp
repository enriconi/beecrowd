#include <iostream>
using namespace std;

int main()
{
  int a, b, c, d, e;

  cin >> a;
  cin >> b;
  cin >> c;
  cin >> d;
  cin >> e;

  cout << (a * 300) + (b * 1500) + (c * 600) + (d * 1000) + (e * 150) + 225 << endl;
  
  return 0;
}